# useobject 로 불러오므로 object 로 감싸지 않는다. 오브젝트 이름은 파일 이름.
name "인트로 배경"
costume 밤하늘 "intro_bg.png" size 960 540
x = 0
y = 0

when scene start do
  # 어두운 상태에서 서서히 밝아진다
  effect_alpha = 100
  repeat 25:
    effect_alpha -= 4
    wait 0.02
  end
end
