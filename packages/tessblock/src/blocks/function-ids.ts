/** Block type names the function editor and the parameter field both need. */
export const DEFINE_BLOCK = 'func_define';
export const PARAM_VALUE = 'func_param_value';
export const PARAM_BOOLEAN = 'func_param_boolean';

/** Blocks that stand for a parameter, in the header and in the body alike. */
export const PARAM_TYPES = [PARAM_VALUE, PARAM_BOOLEAN];
